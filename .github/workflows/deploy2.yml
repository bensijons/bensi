name: CI2
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  success:
    runs-on: ubuntu-latest
    steps:
      - name: success
        run: echo sucessssssccs
